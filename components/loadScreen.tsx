/* eslint-disable @next/next/no-img-element */
const LoadingScreen = () => (
    <div className="fixed inset-0 flex items-center justify-center z-50 bg-black">
      <img src="/CyberCel.png" alt="Loading" />
    </div>
  );
  
  export default LoadingScreen;